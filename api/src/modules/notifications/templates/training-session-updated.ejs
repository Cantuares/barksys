<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #ff9800; color: white; padding: 20px; text-align: center; border-radius: 5px 5px 0 0; }
    .content { background-color: #f9f9f9; padding: 30px; border-radius: 0 0 5px 5px; }
    .session-info { background-color: #fff; padding: 15px; margin: 15px 0; border-left: 4px solid #ff9800; }
    .change-highlight { background-color: #fff3cd; padding: 10px; margin: 10px 0; border-radius: 5px; }
    .old-value { text-decoration: line-through; color: #999; }
    .new-value { color: #ff9800; font-weight: bold; }
    .footer { text-align: center; margin-top: 20px; color: #666; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= title %></h1>
    </div>
    <div class="content">
      <p><%= greeting %></p>

      <p><%= message %></p>

      <div class="session-info">
        <h3><%= sessionDetails %></h3>
        <p><strong><%= packageLabel %>:</strong> <%= packageName %></p>
        <p><strong><%= trainerLabel %>:</strong> <%= trainerName %></p>
      </div>

      <div class="change-highlight">
        <h4><%= changesLabel %></h4>

        <% if (oldDate && newDate) { %>
          <p>
            <strong><%= dateLabel %>:</strong><br>
            <span class="old-value"><%= oldDateFormatted %></span><br>
            <span class="new-value">→ <%= newDateFormatted %></span>
          </p>
        <% } %>

        <% if (oldStartTime && newStartTime) { %>
          <p>
            <strong><%= startTimeLabel %>:</strong><br>
            <span class="old-value"><%= oldStartTime %></span><br>
            <span class="new-value">→ <%= newStartTime %></span>
          </p>
        <% } %>

        <% if (oldEndTime && newEndTime) { %>
          <p>
            <strong><%= endTimeLabel %>:</strong><br>
            <span class="old-value"><%= oldEndTime %></span><br>
            <span class="new-value">→ <%= newEndTime %></span>
          </p>
        <% } %>
      </div>

      <p><%= closingMessage %></p>

      <div class="footer">
        <p><%= footerText %></p>
      </div>
    </div>
  </div>
</body>
</html>
